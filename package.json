{
  "name": "kong-manager-test-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "report:merge": "mochawesome-merge cypress/reports/mochawesome/*.json > cypress/reports/mochawesome/merged.json",
    "report:generate": "marge cypress/reports/mochawesome/merged.json -f index -o cypress/reports/html",
    "report:clean": "rm -rf cypress/reports",
    "test": "npm run report:clean && npm run cy:run && npm run report:merge && npm run report:generate"
  },
  "devDependencies": {
    "cypress": "^15.7.1",
    "cypress-multi-reporters": "^2.0.5",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.0",
    "mochawesome-report-generator": "^6.3.2",
    "typescript": "^5.5.3"
  },
  "private": true,
  "dependencies": {
    "prettier": "^3.7.4"
  }
}
